# Netlify redirects for Sater Advisory
# Format: from  to  status

# Force HTTPS and apex domain (optional - configure in Netlify dashboard if preferred)

# Redirect old URLs from previous site (add any as needed)
# Example: /old-path  /new-path  301

# Trailing slash normalization handled by Netlify automatically.
# These ensure backward compatibility with any QR codes or old links.
/index.html       /            301
/about.html       /about       301
/valuation.html   /valuation   301
/contact.html     /contact     301
/thank-you.html   /thank-you   301
